import React from 'react';
import LoginContainer from '../../Containers/LoginContainer/loginContainer';
import './home.scss';

const Home = (props) => {
  return (
    <div className='home'>
      <LoginContainer />
    </div>
  );
};

export default Home;
